node a = node("take bus"); 
node b = node("brushing teeth"); 
node c = node("take bath"); 
node d = node("breakfast");
node e = node("go to work");
node f = node("wearing shoes");

graph g = a -> b -> c -> d -> e + a -> e + b -> f;

int i=0;
list<node> n = g.getAllNodes();
list<node> tmp = n;
list<node> res = n;
int stmp = 0;
g.setAllUnvisited();

while(i<g.size()){
	if(n.get(i).isVisited()){
		i = i + 1;
	}else{
		tmp = g.dfs(n.get(i));
		stmp = tmp.size();
		while(stmp > 0) {
			res.add(tmp.get(stmp-1));
			stmp = stmp - 1;
		}
		i = i + 1;
	}
}

while(i > 0) {
	print(res.get(i-1+g.size()));
	i=i-1;
}